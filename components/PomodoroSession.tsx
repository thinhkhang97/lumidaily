"use client";

import { PomodoroSession as RefactoredPomodoroSession } from "./pomodoro";

// Re-export the refactored component with the same name
export { RefactoredPomodoroSession as PomodoroSession };
